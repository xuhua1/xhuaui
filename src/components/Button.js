import React from 'react'
import styled from 'styled-components'

const StyledButton = styled.button`
  color: purple;
`

const Button = () => {
  return (
    <StyledButton>npm init</StyledButton>
  )
}

export default Button